<% if logged_in? %>
  <% provide(:title, 'Sub Requests') %>
  <h1 data-toggle="popover" data-html="true"
      data-title="<center>How do I use Sub Requests?</center>"
      data-content=
        "<b>To request a sub,</b> click the <em>request a sub...</em> button
         on the top left of the calendar. <br />
         <b>To cover a request,</b> open the request on the calendar and click
         <em>Swap Shifts</em> to email its poster. <br />
         <b><em>If your request has been covered,</em></b> please delete it from the
         calendar so it's clear which requests are still open.">
   Sub Requests
 </h1>
  <br>
  <%= render 'events/calendar' %>
  <br>
<% else %>
  <%= render 'static_pages/logged_out_home' %>
<% end %>

<script type="text/javascript">
  $(document).ready(function() {
    $('[data-toggle="popover"]').popover({
      placement: 'bottom',
      trigger: 'hover'
    });
  });
</script>
