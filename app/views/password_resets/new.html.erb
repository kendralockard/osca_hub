<% provide(:title, "Forgot password") %>
<h1>Forgot password</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <p style="text-align: center;">No worries! Enter your email below
      & we'll send a password reset link your way.</p>
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
      <%= f.email_field :email, placeholder: "harkguy3000@oberlin.edu", class: 'form-control' %>

      <%= f.submit "Submit", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
