<a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
  <% if mobile? %>
    <b class="mobile_caret caret"></b>
  <% else %>
    <b class="expand_caret caret"></b>
  <% end %>
</a>

<ul class="dropdown-menu menus">
  <%= link_to "delete", menu, method: :delete, style: "color: var(--delete-red);",
                                   data: { confirm: "Delete menu?" } %>
</ul>
